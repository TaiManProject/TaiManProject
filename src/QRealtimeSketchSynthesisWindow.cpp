//#include "QRealtimeSketchSynthesisWindow.h"


//QRealtimeSketchSynthesisWindow::QRealtimeSketchSynthesisWindow(Parameter parameter_p, QWidget *parent, Qt::WindowFlags) :
//    QMainWindow(parent),
//    ui(new Ui::QRealtimeSketchSynthesisWindow)
//{
//    ui->setupUi(this);
//}

//QRealtimeSketchSynthesisWindow::~QRealtimeSketchSynthesisWindow()
//{
//    delete ui;
//}

///*void QRealtimeSketchSynthesisWindow::initWebcam(cv::VideoCapture &videoCapture, int cameraNumber) {
//    videoCapture.open(cameraNumber);
//    if (!videoCapture.isOpened()) {
//        commonTool.log(QString("Error: could not access the camera %1!").arg(cameraNumber));
//    } else {
//        commonTool.log(QString("Loaded default camera %1, start detecting faces!").arg(cameraNumber));
//    }
//}
//*/
